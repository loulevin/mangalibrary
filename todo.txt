x json server installiert
x json server einrichten. npm scripts api npm run api
- rest.client installtion installieren
- db.json erstellen (JavaScript (mangaitems))
- test.rest erstellen
    - ein get 
    - ein post
    - ein delete
- form anpassen um zu speichern
    - axios installieren
- info seite 
    - Mangaliste umbenennen (darstellung der Datenbank)
